# 1. Load the plugin
# @plugin(@varlock/1password-plugin)
#
# 2. Initialize the plugin
# @initOp(token=$ONEPASSWORD_TOKEN)
# ---

# 3. Add a service account token config item (if applicable)
# @type=opServiceAccountToken @sensitive
ONEPASSWORD_TOKEN=

# @sensitive=false
TEST_TOKEN=testing

# @sensitive
PASSWORD_TEST=op(op://Dev/test123/credential)
